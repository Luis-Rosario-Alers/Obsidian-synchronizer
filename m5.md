1. scaffold the repo and write the `CLAUDE.md`: _Verify: CLAUDE.md is created_
2. Create Google Cloud Project: _Verify: Created Google Cloud Project with the name Obsidian Synchronizer_
3. Create the Google Drive API wrapper file: _Verify: I can download, upload, get, and create remote files from Google Drive_
4. Create JSON configuration file based on plan's config options: _Verify: Program uses configuration values in `config.json` to change behavior._
5. Create `Syncher.py` file to parse CLI arguments `--push` and `--pull`: _Verify: program changes behavior based on `--push` or `--pull` arguments_ 
6. Allow `Syncher.py` to ask for OAuth credentials on first usage/if token.json doesn't exist and save it to a file called `token.json`:  _Verify: Program saves OAuth credentials to `token.json` in program directory_
7. Create `watcher.py` using `watchdog` to handle file modification overwatch: _Verify: File modification is detected and the program reacts as a result_
8. Create `uploader.py` to be a daemon that handle Google Drive upload of a changed file: _Verify: When a file is changed, it should be uploaded to google drive_
9. Create `process_monitor.py` using `psutil` to detect the obsidian process: _Verify: The program should close if the obsidian process closes and if the upload queue is empty._ 
10. Create `puller.py` to handle download of remote files to local vault: _Verify: The program should be able to pull remote files from Google Drive to the local obsidian vault._
11. Create `README.md` convering installation and setup steps from the program: _Verify: The repository should have a `README.md` file with installation and setup instructions_